import { Logger } from "./simple-logger";
/**
 * @deprecated
 * After refactoring the only logger that is in use, is the logger passed in object creation settings of Driver.  As
 * fallback logger there use SimpleLogger.
 */
export declare const setupLogger: (_: Logger) => void;
/**
 * @deprecated
 * After refactoring the only logger that is in use, is the logger passed in object creation settings of Driver.  As
 * fallback logger there use SimpleLogger.
 */
export declare const getLogger: () => import("./simple-logger").SimpleLogger;
/**
 * @deprecated
 * After refactoring the only logger that is in use, is the logger passed in object creation settings of Driver.  As
 * fallback logger there use SimpleLogger.
 */
export declare const setDefaultLogger: () => void;
/**
 * @deprecated
 * After refactoring the only logger that is in use, is the logger passed in object creation settings of Driver.  As
 * fallback logger there use SimpleLogger.
 */
export declare const FallbackLogger: () => void;
/**
 * @deprecated
 * After refactoring the only logger that is in use, is the logger passed in object creation settings of Driver.  As
 * fallback logger there use SimpleLogger.
 */
export declare const getFallbackLogFunction: () => void;
//# sourceMappingURL=deprecated.d.ts.map